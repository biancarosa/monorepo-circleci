version: 2.1

orbs:
  python: circleci/python@1.1.0

jobs:
  build:
    executor: python/default
    working_directory: ~/backend/api
    steps:
      - checkout

{% include 'build-api.yml' %}

workflows:
  main:
    jobs:
      - build